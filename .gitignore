# ── Python artifacts ──────────────────────────────────────────────────────────
__pycache__/
*.py[cod]
*.egg-info/
.eggs/
dist/
build/
.venv/
venv/
.env

# ── Scan output & user account data (never commit) ────────────────────────────
archive/
exports/
*.export.json

# ── Reference screenshots — committed as test fixtures ────────────────────────
# These are developer-curated calibration images used by the test suite.
# DO NOT add user scan screenshots or account data here.

# ── Local-only docs (operational runbook, kept off remote) ───────────────────
docs/RUNBOOK.md

# ── Raw packet captures (old research artifacts) ──────────────────────────────
packet_research/

# ── Image/binary assets NOT in youkai/assets/ ─────────────────────────────────
screenshots/

# ── Rust build artifacts ──────────────────────────────────────────────────────
youkai/target/
youkai-portable/
youkai-portable.zip
youkai.exe
youkai-error.log

# ── OS/IDE noise ──────────────────────────────────────────────────────────────
.DS_Store
Thumbs.db
*.swp
*.swo

# ── External reference clone (read-only, not our code) ────────────────────────
irminsul/

# ── Local agent/session state (not shared) ────────────────────────────────────
.claude/

# ── OpenClaw / other agent workspace files ────────────────────────────────────
AGENTS.md
HEARTBEAT.md
IDENTITY.md
SOUL.md
TOOLS.md
USER.md
.openclaw/

# ── Local-only planning docs & project instructions (kept off remote) ──
docs/
CLAUDE.md
